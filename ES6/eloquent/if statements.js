//diaplay the larger
const islarger =(x,y)=>{
    if(x>y){
        console.log(`${x} is larger than ${y}`);
    }
    else{
        console.log(`${x} is less than ${y}`);
        
    }
}

islarger(10,23);
// 10 is less than 23

